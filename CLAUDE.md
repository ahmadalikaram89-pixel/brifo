# Working conventions

- After pushing finished work to a feature branch, open a PR into `main` and merge it right away (squash) — don't wait to be asked. `main` is what the Vercel deployment tracks, so unmerged branches never go live.
